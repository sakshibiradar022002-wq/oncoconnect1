import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.oncoconnect.patient',
  appName: 'OncoConnect',
  webDir: 'public',
  server: {
    androidScheme: 'https',
    // Load from Vercel deployment (works immediately)
    url: 'https://oncoconnect1.vercel.app/patient.html',
    cleartext: true,
    // For local development with Express server, comment out the above and use:
    // url: 'http://10.0.2.2:3000/patient.html',  // Android emulator
    // url: 'http://localhost:3000/patient.html',    // iOS simulator
  },
  plugins: {
    SplashScreen: {
      launchAutoHide: true,
      backgroundColor: '#071210',
      showSpinner: true,
      spinnerColor: '#34d399',
      androidScaleType: 'CENTER_CROP',
      splashFullScreen: true,
      splashImmersive: true,
    },
    StatusBar: {
      style: 'DARK',
      backgroundColor: '#071210',
    },
    Keyboard: {
      resize: 'body',
      style: 'DARK',
    },
  },
};

export default config;
